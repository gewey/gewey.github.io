# VIKTOR SUBSTRATE CHANGELOG

All notable changes to the VIKTOR Substrate will be documented in this file. (Previous history vacuumed to todos.sqlite3)

## [17.5.0-OMEGA] - 2026-05-30

### FIXED: SYNAPSE Intake Pipeline & DHD Standardization ✅

#### Added
- **SYNAPSE Anchor**: Added `SYNAPSE_DIR` as a first-class anchor in [v:\.terminus\dial_home_device.py](v:\.terminus\dial_home_device.py) per Law 4.

#### Changed
- **Session_Intake_Synapse.py**: Resolved `ModuleNotFoundError` and `NameError` by injecting `sys.path` and initializing `VscodeResolverQuark`.
- **md_fluffer_quark.py**:
  - Pointed `CORE_SCRIPT` to `Metadata_Queue_Orchestrator_Synapse.py` (the successor to `METADATA_QUEUE_CORE.py`).
  - Updated `BLACKSET` to include `None` object, ensuring blank fields correctly trigger LLM enrichment.
- **Morning Ritual Optimization**: Updated `scry_files_delta.py` to exclude `.sandbox` folder, significantly reducing I/O during boot.
- **MCP SSOT**: Validated centralized MCP configs in `mcp_populate_settings.py` to prevent `overseer-viktor` crashes.

## [17.5.0-OMEGA] - 2026-05-30

### TODO LEDGER: Historical Vacuum & Orchestrator Deployment ✅

#### Added
- **manage_todo_list.py**: Deployed a Johnny 5 Decoy orchestrator in [v:\.extension\tools\todo/](v:\.extension\tools\todo/).
  - Implements **Sucking Architecture**: Master todo is ingested and cleared automatically.
  - Implements **Permanent Ledger**: All tasks stored in `todos.sqlite3`.
  - Implements **Bulk Vacuum**: Processed 100+ items across 40 historical log files.
- **todos.sqlite3**: Established permanent SQL ledger for task tracking.

#### Changed
- **Substrate Hygiene**: Purged 40+ historical `.changelog.md` and `.session_anchor.md` files after safeguarding data in SQLite.
